export interface UserAdmin {
    id: string;
    name: string;
    email: string;
    // Añade las propiedades que necesites
}

export interface LoginData {
    email: string;
    password: string;
}